spring:
  application:
    name: microservice-one
  cloud:
    config:
      uri: http://localhost:8989
  profiles:
    active:
    - dev
server:
  port: 8080
eureka:
  client:
    service-url:
      default-zone: ${EUREKA_URI:http://localhost:8761/eureka}